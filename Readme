# Probieraufgabe

Create a digital store window with a simple login / user authentication:

## Installation

navigate to server folder and type npm install
same for the client folder navigate there and type npm install

start mongodb

in server folder type npm start and do the same in client folder.
